# Oracle handoff — 2026-08-20, run 1

Written once, never modified. Scope of this run: BL-3, BL-4, ESC-1. All three
were resolved in `docs/DESIGN.oracle.md`: OD-1 (decision), OD-2 (decision),
OD-3 (halt entry).

## Needs planning

- **OD-1 / R1000 — output precision (`format(value, '.12g')`).** Resolves
  ESC-1. No plans exist yet in this repository, so nothing is invalidated.
  Recommendation: fold R1000 into the first plan for milestone `convert`
  (`covers: [R1, R2, R4, R5, R1000]`) rather than opening a separate steward
  plan — the formatting rule lives exactly where those requirements print
  their output, and a separate plan would race the same files. A standalone
  plan under `docs/plans/oracle/` citing OD-1 is also legal if the
  orchestrator prefers it.
- That plan carries three obligations OD-1 names: exact-string CI tests
  including the ESC-1 reproduction (`0.1 km → m` prints `100`, observed red
  then green), exact formatted strings in `acceptance/S1.sh` and
  `acceptance/S2.sh`, and — once the check has been observed — the ESC-1
  correction row in `docs/escapes.md`.

## Do not act on

- **OD-2 (BL-4, currency).** Resolved by declining: the design's §3 non-goal
  and R8 stand, no requirement was added, and there is nothing to plan or
  build. Do not schedule currency work; if the owner still wants it, they
  edit `docs/DESIGN.md` themselves and the next oracle run supersedes OD-2.
- **OD-3 (BL-3, `rich` output) — HALT.** Do not plan any output-styling work,
  with or without `rich`, until the owner acts: BL-3 demands a runtime
  dependency that core tenet V5 forbids, and both are the owner's words. The
  halt entry says the two smallest unblocks (amend V5, or supersede BL-3 with
  a stdlib-only ask). The run continues past this; only BL-3 is parked.

## Notes for the orchestrator

- `docs/DESIGN.md` §11 also leaves the CLI argument syntax and the batch line
  format undecided. Neither has logged evidence yet, so this run rules on
  neither — the planner should file them as `BL-<n>` uncertainties (HIGH:
  both are external formats) when planning `convert`, per `AGENTS.md`.
