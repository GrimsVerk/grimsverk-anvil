# Oracle handoff — 2026-08-20, run 1

Scope commissioned: BL-3, BL-4, ESC-1. All three are resolved in
`docs/DESIGN.oracle.md` (OD-1, OD-2, OD-3). Written once; a correction, if one
is ever needed, goes in the next-numbered file.

## Needs planning

- **OD-1 / R1000 — output precision** (resolves ESC-1). One requirement:
  results print at 12 significant digits, trailing zeros stripped
  (`format(value, ".12g")` semantics). No plan exists yet under `docs/plans/`,
  so this is not a change to landed work — it belongs in the print layer of
  the MVP `convert` milestone. Two acceptable routes: fold `covers: [R1000]`
  into the `convert` milestone plan when it is written, or have the steward
  cut a plan under `docs/plans/oracle/` citing OD-1. Whichever plan carries it
  must also carry the measurement the decision names: `acceptance/S1.sh`'s
  fixed table includes the ESC-1 case — `0.1 km` to `m` prints exactly `100`.
  Once that has merged, append ESC-1's correction row in `docs/escapes.md`
  naming the demonstrated check (not the oracle's write — any agent may append
  the ledger row per its own rules).

## Do NOT act on

- **BL-3 (OD-2, HALTED).** Granting it requires the `rich` dependency that
  core tenet V5 forbids, and the item is owner-filed against the owner's own
  tenet — only the owner can resolve that, by editing V5 or re-filing BL-3
  without the dependency. Do not plan it, do not build a stdlib approximation
  of it (the item explicitly rules that out), and do not treat the halt as a
  rejection: it is an open question parked with the owner. The run continues
  past it.
- **BL-4 (OD-3, rejected).** No work exists downstream: no requirement was
  added, no plan is needed, and the design's offline non-goal stands. If
  anyone re-proposes currency it goes through a new backlog item, not through
  reopening OD-3.

## Existing plans touched

None. `docs/plans/` holds only the template; nothing landed is affected by any
of these decisions.

## For the owner's attention (via the run report, not for the orchestrator)

- OD-2 is waiting on one sentence from you about V5 versus BL-3.
- OD-1 fixed the digit count at 12; the rule's intent is solid, the count is a
  judgment call — edit V1's emphasis or say a number if you want a different
  one, and a superseding decision follows.
