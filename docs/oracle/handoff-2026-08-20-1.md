# Oracle handoff — 2026-08-20, run 1

Commissioned unattended by the delivery driver. Scope: BL-3, BL-4, ESC-1.
Decisions written this run: OD-1, OD-2, OD-3 (`docs/DESIGN.oracle.md`).
Written once; a correction, if ever needed, is the next-numbered file.

## Needs planning

- **OD-1 / R1000 — output precision (resolves ESC-1).** No plan exists yet in
  `docs/plans/` (only the template), so there is nothing to amend — R1000 should
  be folded into the plan for the MVP `convert` milestone when that plan is
  written, since output formatting lives exactly where that milestone's CLI
  prints results. A separate steward plan under `docs/plans/oracle/` citing OD-1
  is the fallback if the milestone plan is already frozen by then. Whichever
  plan carries it must also carry the decision's measurement clause: S1's
  acceptance fixture gains the exact-string row `0.1 km` → `m` = `100`, S1/S2/S5
  fixtures assert exact output strings, and after the check is observed red
  against the unrounded behaviour and green against the fix, the implementing
  work appends ESC-1's correction row in `docs/escapes.md`.

## Nothing to plan, and do not act

- **OD-2 (BL-3, `rich` / pretty output) — refused.** Adds no requirement.
  Do not plan, build, or partially build pretty output in any form, including a
  stdlib hand-rolled variant; the refusal covers the feature, not just the
  dependency. The item stays in Proposed. It reopens only by the owner editing
  V5 in `docs/VISION.md`, after which a superseding decision citing BL-3 can be
  written.
- **OD-3 (BL-4, currency with live rates) — refused.** Adds no requirement.
  Do not plan or build currency in any form, including bundled static rates.
  It reopens only by the owner amending `docs/DESIGN.md` (§3 non-goals, R8) or
  the vision.

## Out of scope this run — untouched, still open

- **BL-1** (unit aliases) and **BL-2** (below absolute zero): not in this run's
  scope and not ruled on. They remain Proposed; per `docs/BACKLOG.md` they are
  never coded unprompted. Route them to a future oracle run if the driver wants
  them ruled.
- `docs/DESIGN.md` §11 also leaves the exact CLI argument syntax and the
  batch-mode line format undecided. No `BL-<n>` uncertainty has been filed for
  either yet — that is the planner's job when the first plan is written, and
  ruling on the filings is a future oracle run's. Nothing for the orchestrator
  to act on now.

## For the report upward

The vision decided everything in scope cleanly; no halt was needed. OD-2 sits
closest to the halt line (owner-filed item vs. owner-written tenet) and was
ruled a refusal because refusing violates no tenet — the tenet itself decides.
