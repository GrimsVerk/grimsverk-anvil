# Oracle handoff — 2026-08-20, run 1

Scope commissioned: BL-3, BL-4, ESC-1. All three are resolved in
`docs/DESIGN.oracle.md` (OD-1, OD-2, OD-3). Written once; corrections go in the
next-numbered file.

## Needs planning

- **OD-1 / R1000** (resolves ESC-1) — the output precision rule:
  `format(value, ".12g")` on every printed result, single-shot and batch.
  No plans exist yet, so there is no plan to amend: the plan that covers the
  MVP `convert` milestone must list **R1000** in its `covers` alongside R1, R2,
  R4, R5. The plan must also carry the measurement OD-1 names: unit tests
  pinning the rule, S1's fixed-table acceptance script written with exact-match
  expected strings including the ESC-1 case (`0.1 km` to `m` prints exactly
  `100`), and — after that merges — the completing correction row for ESC-1 in
  `docs/escapes.md` citing the script.

## Do NOT act on

- **BL-3 (OD-2, HALTED)** — the request for `rich` collides with core tenet V5
  (no runtime dependencies), and the item as filed rejects the stdlib
  alternative. Blocked on the owner: amend V5 with an exception, or withdraw or
  re-file BL-3. Do not plan it, and do not build a stdlib approximation of it.
- **BL-4 (OD-3, rejected)** — currency stays a non-goal; the design is
  unchanged. Nothing to plan. Do not treat the rejection as an invitation to a
  scoped-down variant; a new proposal would need new owner-filed evidence.

## Notes for the orchestrator

- OD-3 adds no requirements, so coverage is unaffected by it.
- ESC-1 stays open (stub, check pending) until the work in OD-1's measurement
  paragraph merges; only then append its correction row and record it in
  `escapes.done.md`.
