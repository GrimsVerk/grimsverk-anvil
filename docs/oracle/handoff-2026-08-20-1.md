# Oracle handoff — 2026-08-20, run 1

Written once, never modified. Scope of this run: BL-3, BL-4, ESC-1. Three
entries landed in `docs/DESIGN.oracle.md`: OD-1 (a decision that needs
planning), OD-2 (a decline that needs nothing), OD-3 (a halt that waits on the
owner).

## Needs planning

**OD-1 / R1000 — the 12-significant-digit output rule.** No plans exist yet
(`docs/plans/` holds only the template), so no existing plan is touched.
Recommended shape: fold R1000 into the first milestone plan (`convert`, per
`docs/DESIGN.md` §12) rather than a standalone steward plan — the formatting
rule has no observable surface until the CLI prints something, and a separate
plan would just re-touch the same files. The `convert` plan's `covers:` list
then includes R1000 alongside R1, R2, R4, R5. A steward plan under
`docs/plans/oracle/` citing OD-1 is also legal if the milestone plan has
already been cut when this is read.

Whichever shape, the plan must carry the measurement OD-1 names:

- the ESC-1 reproducer as a row in `acceptance/S1.sh`'s fixed table — 0.1 km
  to m prints exactly `100`;
- unit tests asserting the formatting rule at representative magnitudes;
- after both merge, the completing correction row for ESC-1 in
  `docs/escapes.md`, naming them.

## Do NOT act on

- **BL-4 (currency)** — declined by OD-2. Do not plan it, do not build it; the
  design's non-goal stands. It is resolved by decision, not by work.
- **BL-3 (rich tables)** — OD-3 halted on tenet V5. Do not plan it, do not add
  `rich`, and do not build a stdlib substitute either — the halt records why a
  substitute would misrepresent the ask. It waits on the owner: either an
  exception clause in V5 (plus the dependency approval `AGENTS.md` requires),
  or a new backlog item superseding BL-3 that asks for stdlib-only output.
- **BL-1 (aliases) and BL-2 (below absolute zero)** — outside this run's
  commissioned scope, sitting in Proposed with no approval recorded and no
  contradiction logged against a landed requirement. They remain for the owner
  or a future scoped run; nothing here rules on them.

## For the owner (report material, not orchestrator work)

- OD-3's needed ruling, above.
- The vision's purpose sentence ("answers instantly and offline") carries no
  V-id, so decisions cannot cite it as the statement relied on. OD-2 leaned on
  V3 where this sentence was the truer ground. Giving it an id would let
  future decisions cite the actual reason.
