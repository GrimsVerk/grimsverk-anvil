# Oracle handoff — 2026-08-20, run 1

Scope worked: BL-3, BL-4, ESC-1. Three entries landed in
`docs/DESIGN.oracle.md`: OD-1 (decision), OD-2 (decision, declines work),
OD-3 (halt).

## Needs planning

- **OD-1 / R1000** — output formatting at 12 significant digits
  (`format(value, '.12g')`), resolving ESC-1.

  No plans exist yet, so there is nothing to retrofit — and that is the point
  of saying it now: whichever plan builds milestone `convert` (R1, R2, R4, R5)
  must fold R1000's formatting in from the first slice rather than bolting it
  on later. Either a steward plans R1000 under `docs/plans/oracle/`, or the
  milestone `convert` plan lists `covers: [R1000]` alongside its own ids;
  one of the two, not both.

  The plan carries the measurement, per the decision: blind tests asserting
  exact printed strings (including `0.1 km` → `m` printing `100`), the same
  case in `acceptance/S1.sh`'s fixed table as an exact-string expectation, and
  — only after the check has been seen red against the defect and green against
  the fix — the ESC-1 correction row in `docs/escapes.md`.

## Do NOT act on

- **BL-4 (currency)** — declined by OD-2. No requirement exists, so no plan may
  cover it. Reopening it is two owner edits (`docs/DESIGN.md` §3/R8 and the
  vision's priority order), not agent work.
- **BL-3 (rich tables)** — halted by OD-3. It collides with core tenet V5 and
  only the owner can resolve which of their two statements wins. Do not plan
  it, and do not build a stdlib "compromise" version — that would be acting on
  evidence the halt says cannot be ruled on as filed.

## Not decided this run (out of scope, still open)

- **BL-1** (unit aliases) and **BL-2** (below absolute zero) were not in this
  run's scope and remain unruled. Both look decidable on existing vision
  statements (V2, V3, V4); hand them to the next oracle run.
- The command-line syntax and the batch line format remain deliberately open
  (`docs/DESIGN.md` §11). Planning files those as uncertainties per
  `AGENTS.md`; they were not ruled here because no `BL-<n>` has filed them yet.
