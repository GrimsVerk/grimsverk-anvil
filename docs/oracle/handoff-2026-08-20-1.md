# Oracle handoff — 2026-08-20 — run 1

Scope given by the delivery driver: ESC-1, BL-3, BL-4. Three decisions landed
in `docs/DESIGN.oracle.md`: OD-1, OD-2, OD-3. No criterion was waived and no
halt was written.

## Needs planning

- **OD-1 (resolves ESC-1) — R1000, the 12-significant-digit output rule.** The
  only decision that adds work. No plans exist yet, so nothing is invalidated.
  Two acceptable shapes, whichever the dispatch order produces first: a steward
  plan under `docs/plans/oracle/` citing OD-1, or the first milestone plan
  (`convert`: R1, R2, R4, R5) carrying R1000 in its `covers:` list — the rule
  is one formatting call on the output path, and it pairs naturally with the
  milestone that builds that path. Whichever shape lands, the plan must carry
  the measurement OD-1 names: unit tests asserting exact printed strings
  (including 0.1 km → m printing exactly `100`), and the S1/S2 acceptance
  scripts comparing exact stdout strings rather than parsed floats. After the
  fix and its check merge, the implementer appends the ESC-1 correction row in
  `docs/escapes.md`; the stub's check column is waiting on exactly this.

## Needs nothing

- **OD-2 (resolves BL-3)** — declined. No plan, no code. Do not add `rich`,
  and do not build a standard-library substitute for it either; output stays
  plain. The owner's steer, if they disagree, is editing V5.
- **OD-3 (resolves BL-4)** — declined. No plan, no code. Currency remains a
  non-goal; a currency symbol stays a loud R4 unknown-unit error. The owner's
  steer, if they disagree, is editing `docs/DESIGN.md` §3/§5.

## Do not act on yet

- The two other open questions in `docs/DESIGN.md` §11 — exact CLI syntax and
  the batch line format — are still open. OD-1 settled only output precision;
  the planner files the other two as uncertainties when planning reaches them.
- Two further Proposed backlog items (unit aliases; temperatures below
  absolute zero) remain unruled. They are deliberately not named by id in this
  file: the driver records every id a handoff mentions as processed evidence,
  and these have not been processed — they should reach the oracle in a later
  run through the normal evidence detection.
