# Oracle handoff — 2026-08-20 — 1

Run scope: BL-3, BL-4, ESC-1 (delivery driver, unattended). All three ruled;
decisions OD-1..OD-3 in `docs/DESIGN.oracle.md`. Written once; corrections go
in the next-numbered file.

## Needs planning

- **OD-1 → R1000** (output formatting: `format(value, ".12g")`, so 0.1 km → m
  prints exactly `100`). No plans exist yet, so the simplest correct route is
  for the first milestone plan (`convert` — MVP, R1, R2, R4, R5) to list
  `covers: [..., R1000]` and build the formatting rule into the result path
  from the start; a separate steward plan under `docs/plans/oracle/` citing
  OD-1 is also legal but would serialize two PRs for one output line. The plan
  carries, as part of the same work, the measurement OD-1 names:
  - `acceptance/S1.sh`'s fixed table includes the ESC-1 reproduction (0.1 km →
    m, expected stdout exactly `100`, exact-string comparison);
  - unit tests assert the `.12g` formatting on the result path;
  - once that script has been observed red against the defect and green
    against the fix, append the ESC-1 correction row in `docs/escapes.md` and
    the closure row in `docs/escapes.done.md` naming `acceptance/S1.sh`.

## Existing plans touched

None — `docs/plans/` holds only the template. R1000 constrains every future
plan that prints a result (MVP `convert`, `temperature`, `convert-batch`):
result lines go through the one formatting rule.

## Do NOT act on

- **BL-3** (rich tables) and **BL-4** (live currency) — declined (OD-2, OD-3).
  No plan, no code, no backlog approval rows. They stay in Proposed
  (append-only); reversal is the owner's, by editing `docs/VISION.md` V5 or
  `docs/DESIGN.md` §3/R8, after which a re-filed item can be ruled differently.
- **BL-1** (unit aliases) and **BL-2** (below absolute zero) — not declined,
  simply outside this run's scope. They remain open evidence for a future
  oracle run; do not treat them as ruled either way, and do not plan them —
  the design layer does not name them yet.
- The CLI argument syntax and batch line format remain deliberately undecided
  (`docs/DESIGN.md` §11). The planner should file them as `BL-<n>`
  uncertainties per the planning rule when the MVP plan is written, not guess
  silently — ESC-1 covered precision only.
