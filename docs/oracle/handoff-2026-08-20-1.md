# Oracle handoff — 2026-08-20, run 1

Unattended run, commissioned by the delivery driver. Scope handed in: BL-3,
BL-4, ESC-1. All three are resolved by decisions OD-1..OD-3 in
`docs/DESIGN.oracle.md`; nothing in scope was left undecided and no halt was
written.

## Needs planning

- **OD-1 / R1000 (output precision, from ESC-1).** The only decision that adds
  work. No plans exist yet, so R1000 should ride the milestone planning rather
  than get a plan of its own: whichever plan covers the MVP `convert` milestone
  (R1, R2, R4, R5) should list R1000 in its `covers:` too — the formatting rule
  is the print path of the same CLI, and a standalone formatting plan would be
  a horizontal sliver of the kind `AGENTS.md` forbids. The plan must carry the
  two measurements OD-1 names: the ESC-1 case (0.1 km to m prints exactly
  `100`) inside the fixed table `acceptance/S1.sh` checks, and a unit test for
  the rule demonstrated red against the unrounded output. After both merge, the
  ESC-1 correction row is appended to `docs/escapes.md`.

## Touches existing plans

None — `docs/plans/` holds only the template. Nothing already planned is
invalidated by any of these decisions.

## Do NOT act on

- **OD-2 and OD-3 authorize nothing.** They are rejections; no code, no plan,
  and no backlog edit follows from them. BL-3 and BL-4 stay where they are —
  `docs/BACKLOG.md` is append-only, and their resolution lives in the ledger.
- **BL-1 (unit aliases) and BL-2 (below absolute zero)** were outside this
  run's scope and are unruled. They remain Proposed: never coded unprompted,
  and not to be treated as settled either way until a decision cites them.
- **No waiver was issued.** No acceptance criterion's handling changes;
  `docs/acceptance.md` is untouched by this run.

## For the next oracle run

BL-1 and BL-2 are the obvious next evidence to work; BL-2 in particular
interacts with OD-1's claim that temperature conversion is at most one
multiplication and one addition (a range check would be a second branch, not
more float error, so OD-1 does not pre-decide it).
