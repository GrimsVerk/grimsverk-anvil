# Oracle handoff — 2026-08-20, run 4

Scope commissioned: BL-7. Resolved in `docs/DESIGN.oracle.md` as **OD-6**.
Written once; corrections go in the next-numbered file.

## Needs planning

Nothing. **OD-6** (resolves BL-7) ratifies the default the temperature plan
proceeded on: `docs/plans/oracle/anvil-temperature.md` carries **R7** and
**R8** alongside R3, and its `covers` line already says so. The ruling is a
zero-diff ratification — no plan amendment, no slice change, no script change,
no new requirement. The build queue is unchanged: the MVP plan
(`docs/plans/oracle/anvil-convert-mvp.md`) first, the temperature plan second.

One consequence worth knowing: once both plans are in the tree — they already
are — `coverage.sh` reports every requirement except R6 covered. R6 (batch
mode) is the remaining planning gap, and it is blocked on its milestone, not on
any ruling.

## Deliberately NOT decided

- **The batch line format** (`docs/DESIGN.md` §11, third open question) —
  unchanged from earlier handoffs: the `convert-batch` plan must file it as its
  own `BL-<n>` before writing S5.
- **S4 stays the owner's.** OD-6 assigns which plan schedules R7 and R8; it
  neither creates agent-executable evidence for them nor waives anything. The
  S4 row in `docs/acceptance.md` remains `pending / owner`.

## Do NOT act on

- **BL-3 (OD-2, HALTED)** — unchanged: blocked on the owner reconciling BL-3
  with tenet V5. Do not plan it or approximate it.
- **BL-4 (OD-3, rejected)** — unchanged: currency stays a non-goal.
- **BL-1, BL-2** — Proposed, owner-filed, not commissioned: they move only
  when the owner moves them.

## Notes for the orchestrator

- OD-6 corrects, in its alternatives field, a factual error in BL-7 as filed:
  the temperature plan is not the first whose acceptance script runs the
  installed command — the MVP's S1 does too. The default still stands; the
  correction is recorded so nobody later treats that clause as precedent for
  where non-functional requirements belong.
- ESC-1 remains open (stub) until the OD-1 measurement work merges, as
  handoff run 1 recorded.
