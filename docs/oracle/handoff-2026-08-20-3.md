# Oracle handoff — 2026-08-20, run 3

Scope commissioned: BL-6, BL-7, BL-8. All three resolved in
`docs/DESIGN.oracle.md` (OD-5, OD-6, OD-7). Written once; a correction, if one
is ever needed, goes in the next-numbered file.

## Needs planning

**The MVP `convert` milestone plan, now unblocked.** BL-6 and BL-7 were the
HIGH uncertainties that stopped it; OD-5 and OD-6 rule them, so the planner now
derives instead of guessing:

- **Signatures and command lines** come from **R1001** (OD-5): three
  positionals in the order value, from-unit, to-unit; `--batch` reserved (a
  usage error until R6 is built); a bare `anvil` is a usage error; exact,
  case-sensitive §5 unit symbols.
- **Output** comes from **R1002** (OD-6): the formatted value alone on
  standard output, one line; errors and usage on standard error; a failed
  invocation writes nothing to standard output.
- **`covers:`** claims `[R1, R2, R4, R5, R7, R8, R1001, R1002]` — R7 and R8
  per OD-7, R1001 and R1002 because they landed this run and live in this
  milestone; no other plan will ever carry them.
- **Acceptance scripts:** `acceptance/S1.sh` compares the *entire* stdout line
  against each expected value (OD-6's measurement obligation), and
  `acceptance/S3.sh` includes the bare `anvil` case among its missing-argument
  invocations and asserts stderr carries the message while stdout stays empty.

## Existing plans touched

- `docs/plans/oracle/od1-output-precision.md` — untouched, confirmed. OD-6
  makes its asserted string exact: for the ESC-1 case, the "printed value
  field" is the whole stdout line, `100`. Build order unchanged: the `convert`
  milestone's work first, then that plan.
- **The future `convert-batch` plan (R6)** now carries three inherited
  obligations: the two from OD-4 (result lines print through `format_result`;
  a precision-sensitive ESC-1 row in `acceptance/S5.sh`), plus OD-5's
  invocation form (`anvil --batch`, no positionals). The **batch line format
  is still undecided** — `docs/DESIGN.md` §11 leaves it open and no logged
  evidence has reached the oracle — so the batch planner files it as an
  uncertainty (`BL-<n>`, HIGH: it is an external format) rather than settling
  it. No plan for R6 exists yet; nothing is commissioned now.

## Do NOT act on

- **BL-1 (aliases), BL-2 (below absolute zero).** Still unruled; no decision
  cites them, so nothing may plan them. Note for whoever files evidence next:
  OD-5 deliberately pinned exact §5 symbol matching *because* BL-1 is unruled,
  and a future ruling on BL-1 supersedes that one sentence of R1001 only.
- **BL-3 (OD-2, HALTED).** Still parked with the owner — one sentence on V5
  versus BL-3. Do not plan or approximate it.
- **BL-4 (OD-3, rejected).** Closed; nothing to do.

## For the owner's attention (via the run report, not for the orchestrator)

- Nothing is needed from you to keep the run moving. OD-7 used the no-vision
  class — coverage bookkeeping, the class that field exists for; the reasoning
  lives in its alternatives field.
- One vision gap worth a sentence someday: the vision never says who the
  output is *for*. OD-6 had to lean on `docs/DESIGN.md` §4's script-author use
  case to choose bare-value output; a vision statement like "output is for
  pipes first, eyes second" would have decided BL-7 directly — and would also
  bear on BL-3 whenever you unhalt it.
