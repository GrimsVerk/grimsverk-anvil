# Oracle handoff — 2026-08-20, run 3

Scope commissioned: BL-6. Resolved in `docs/DESIGN.oracle.md` as **OD-5 /
R1002**. Written once; corrections go in the next-numbered file.

## Needs planning

- **OD-5 / R1002** (resolves BL-6) — non-finite values are refused on the R4
  path: an input that parses to `inf`/`-inf`/`nan`, and a conversion of finite
  inputs that overflows to infinity, both print an error naming the offending
  input on standard error and exit non-zero. The tool never prints `inf` or
  `nan` as a conversion result. This is the default the MVP plan
  (`docs/plans/oracle/anvil-convert-mvp.md`, status draft) already proceeded
  on, so no new plan is needed — the existing plan needs a small amendment:
  - add **R1002** to its `covers` list;
  - its slice-2 description of `acceptance/S3.sh` lists only the §13 cases —
    add the two non-finite rows (`anvil inf km m`, `anvil 1e308 km mm`) to the
    rows the script executes. That is the measurement OD-5 names; the plan's
    message-contract table already contains both rows, so this is one line.
  No slice boundary, Signatures block, or S1 table changes.

## Deliberately NOT decided

- **The batch line format** (`docs/DESIGN.md` §11, third open question) —
  unchanged from handoff run 2: the `convert-batch` plan must file it as its
  own `BL-<n>` before writing S5. R1002 already binds batch result lines
  (never `inf`/`nan`), but the line *format* remains open.
- **Exact error-message wording** for the two refusals — R4 delegates it and
  R1002 keeps it with the plan; the plan's slice-2 table wording stands unless
  the plan itself changes it.

## Do NOT act on

- **BL-3 (OD-2, HALTED)** — unchanged: blocked on the owner reconciling BL-3
  with tenet V5. Do not plan it or approximate it.
- **BL-4 (OD-3, rejected)** — unchanged: currency stays a non-goal.
- **BL-1, BL-2** — Proposed, owner-filed, not commissioned: they move only
  when the owner moves them.

## Notes for the orchestrator

- OD-5 confirms the direction the draft plan took, so nothing built so far is
  invalidated; the amendment above is bookkeeping plus one script addition,
  not a re-plan.
- ESC-1 remains open (stub) until the OD-1 measurement work merges, as
  handoff run 1 recorded.
