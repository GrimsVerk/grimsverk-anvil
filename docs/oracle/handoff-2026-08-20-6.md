# Oracle handoff — 2026-08-20, run 6

Scope commissioned: BL-9. Resolved in `docs/DESIGN.oracle.md` as **OD-8**,
adding **R1004**. Written once; corrections go in the next-numbered file.

## Needs planning

- **No new plan.** OD-8 lands inside the existing
  `docs/plans/oracle/anvil-convert-batch.md`, which already proceeded on the
  filed default (slice 1 reads standard input with `errors="replace"`). What it
  needs is a small amendment on its own `docs/` branch, before the batch
  milestone is dispatched to build:
  - add **R1004** to the plan's `covers` (otherwise `coverage.sh` reports
    R1004 unplanned and the loop stalls on it);
  - add the unit-test rows OD-8's measurement paragraph names: tests that
    drive `main(["--batch"])` with real undecodable bytes on a patched binary
    standard input, asserting the error line under an existing reason, the
    later line's correct result, exit 1, and empty standard error. Tests
    through `run_batch` with `io.StringIO` cannot observe this — the decode
    happens in `main` — which is why the amendment is required rather than
    nice-to-have.
- `acceptance/S5.sh` is untouched by OD-8: no expected byte changes, no new
  batch, exactly as BL-9's LOW risk class stated.

## Existing plans this touches

- `docs/plans/oracle/anvil-convert-batch.md` — the amendment above, and
  nothing else: no slice boundary, no Signatures block, no acceptance script.
- `docs/plans/oracle/anvil-convert-mvp.md` and
  `docs/plans/oracle/anvil-temperature.md` — unchanged. Build order stands:
  MVP first, temperature second, batch third.

## Do NOT act on

- **BL-3 (OD-2, HALTED)** — unchanged: blocked on the owner reconciling BL-3
  with tenet V5. Do not plan it or approximate it.
- **BL-4 (OD-3, rejected)** — unchanged: currency stays a non-goal.
- **BL-1, BL-2** — Proposed, owner-filed, not commissioned: they move only when
  the owner moves them.

## Notes for the orchestrator

- ESC-1 remains open (stub) until OD-1's item (c) — the correction row citing
  the merged `acceptance/S1.sh` — lands as its own one-line `docs/` pull
  request after the MVP merges. Unchanged from handoff run 1.
- With OD-8 landed, every uncertainty filed to date (BL-5 through BL-9) is
  cited from a decision, and no planning gap remains that this ledger knows
  of: the batch plan amendment above is the only outstanding oracle-side
  follow-up before the three plans build in order.
- Reversal cost if the owner prefers strict decoding with a dedicated
  "not valid UTF-8" reason: one superseding entry, one line in `main`, plus
  the new reason's wording and tests — OD-8's alternative (3), recorded there
  with why it lost.
