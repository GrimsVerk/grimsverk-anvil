# Oracle handoff — 2026-08-20, run 2

Written once, never modified. Scope of this run: BL-5 (the steward-filed
uncertainty: which plan carries R1000, and under what invocation syntax). Two
entries landed in `docs/DESIGN.oracle.md`: OD-4 and OD-5. Both cite BL-5, which
therefore leaves "Uncertainties awaiting oracle ruling"; nothing is appended to
`docs/BACKLOG.md` itself.

## Needs planning

**One plan: the `convert` milestone plan** (slug `convert`, at
`docs/plans/convert.md`, the normal owner-reviewed path — NOT
`docs/plans/oracle/`; OD-4 rules that no steward plan is cut for OD-1).

- `covers: [R1, R2, R4, R5, R1000, R1001]` — `docs/DESIGN.md` §12's MVP scope
  plus the two oracle requirements.
- The invocation syntax is now ruled (OD-5 / R1001:
  `anvil <value> <from-unit> <to-unit>`, value alone on stdout formatted per
  R1000, diagnostics on stderr, exit 0 on success). **The planner does not
  re-file the syntax as an uncertainty** — that is the round-trip OD-5 exists
  to avoid.
- The plan must carry the measurements OD-1 and OD-5 name: `acceptance/S1.sh`
  with its fixed table including the ESC-1 reproducer row (0.1 km to m prints
  exactly `100`), `acceptance/S3.sh` for the R4/R5 error and usage cases, and
  unit tests asserting the R1000 formatting rule at representative magnitudes
  and the stdout/stderr separation.
- After the implementation and its checks merge, ESC-1 gets its completing
  correction row in `docs/escapes.md`, naming them — that closes its pending
  check column and earns its `escapes.done.md` line.

## Do NOT act on

- **Batch mode's invocation and line format** — still deliberately open in
  `docs/DESIGN.md` §11. R1001 explicitly does not decide them; they belong to
  the `convert-batch` milestone's planning round, which files them when it
  comes.
- **BL-1 (aliases), BL-2 (below absolute zero)** — still Proposed, no approval
  recorded, no ruling; outside this run's scope.
- **BL-3 (rich tables)** — halted by OD-3 on tenet V5; waits on the owner.
- **BL-4 (currency)** — declined by OD-2; resolved by decision, not by work.

## For the owner (report material, not orchestrator work)

- OD-5 leaned on V3 (simplicity), but the truer ground — output built for
  pipes and scripts first — has no statement in `docs/VISION.md` to cite. A
  sentence about scriptability would let future interface decisions cite the
  actual reason. Related: handoff run 1 already noted the purpose sentence
  ("answers instantly and offline") carries no V-id.
- OD-3's needed ruling (handoff run 1) still waits.
