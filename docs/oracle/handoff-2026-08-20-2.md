# Oracle handoff — 2026-08-20, run 2

Scope commissioned: BL-5. Resolved in `docs/DESIGN.oracle.md` as **OD-4 /
R1001**. Written once; corrections go in the next-numbered file.

## Needs planning

- **OD-4 / R1001** (resolves BL-5) — the CLI syntax is fixed:
  `anvil <value> <from-unit> <to-unit>` single-shot; `anvil --batch` (no
  positionals, reads stdin) for batch; every other shape — including a bare
  `anvil` — prints usage to standard error and exits non-zero. This unblocks
  the plan covering the MVP `convert` milestone: it must list **R1001** in its
  `covers` alongside R1, R2, R4, R5, R1000, declare its Signatures block
  against this syntax, and write the S1 and S3 acceptance scripts executing
  these exact command lines. S3 must include the bare-`anvil` invocation as an
  explicit row — that is the measurement OD-4 names.
- No plans exist yet under `docs/plans/`, so nothing is amended; the first MVP
  plan simply starts from this syntax.

## Deliberately NOT decided

- **The batch line format** (`docs/DESIGN.md` §11, third open question) — BL-5
  asked only how batch is *invoked*, and OD-4 rules only that. What a request
  line and an error line look like is still open. The plan covering the
  `convert-batch` milestone (R6, S5) must file it as a new `BL-<n>` uncertainty
  before writing S5, not settle it silently.

## Do NOT act on

- **BL-3 (OD-2, HALTED)** — unchanged from handoff run 1: blocked on the owner
  reconciling BL-3 with tenet V5. Do not plan it or approximate it.
- **BL-4 (OD-3, rejected)** — unchanged from handoff run 1: currency stays a
  non-goal, nothing to plan.
- **BL-1, BL-2** — Proposed items, owner-filed, not commissioned this run and
  not uncertainties: they move only when the owner moves them.

## Notes for the orchestrator

- OD-4 requires no `--batch` implementation in the MVP: until the
  `convert-batch` milestone, `--batch` is an unrecognised argument and takes
  the R5 usage-error path, which R1001 makes consistent by construction.
- ESC-1 remains open (stub) until the OD-1 measurement work merges, as
  handoff run 1 recorded.
