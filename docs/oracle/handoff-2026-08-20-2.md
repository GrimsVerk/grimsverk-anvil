# Oracle handoff — 2026-08-20, run 2

Scope commissioned: BL-5. Resolved in `docs/DESIGN.oracle.md` (OD-4). Written
once; a correction, if one is ever needed, goes in the next-numbered file.

## Needs planning

Nothing. OD-4 adds no requirement and changes no landed plan — it confirms the
filed default: `docs/plans/oracle/od1-output-precision.md` claims
`covers: [R1000]` and delivers the single-shot half. The plan proceeds exactly
as merged, in the order its own summary fixes: the `convert` milestone's
conversions first, then this plan.

## Existing plans touched

- `docs/plans/oracle/od1-output-precision.md` — confirmed, not changed. No
  frontmatter edit, no re-plan.
- **The future `convert-batch` plan (R6) inherits two obligations from OD-4**,
  recorded so its planner derives them instead of guessing: (1) batch result
  lines print through `format_result`; (2) `acceptance/S5.sh`'s batch fixture
  carries a precision-sensitive row — the ESC-1 case as a batch line whose
  result field prints exactly `100`. That is how the batch half of R1000
  becomes observed. No plan for R6 exists yet; this binds it when it is
  written, it commissions nothing now.

## Do NOT act on

- **BL-3 (OD-2, HALTED).** Still parked with the owner — one sentence on V5
  versus BL-3. Unchanged by this run; do not plan or approximate it.
- **BL-1, BL-2 (aliases, absolute zero).** Outside this run's scope and still
  unruled. No decision cites them, so nothing may plan them yet.

## For the owner's attention (via the run report, not for the orchestrator)

- Nothing new is needed from you. OD-4 used the no-vision class — the vision
  genuinely does not speak to coverage bookkeeping, which is the class that
  field exists for; the reasoning lives in its alternatives field.
