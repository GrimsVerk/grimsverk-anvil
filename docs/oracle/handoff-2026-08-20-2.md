# Oracle handoff — 2026-08-20 — run 2

Scope given by the delivery driver: BL-5. One decision landed in
`docs/DESIGN.oracle.md`: OD-4, adopting the steward's proposed default. No
criterion was waived, no halt was written, no new requirement was added.

## Needs planning

- **OD-4 (resolves BL-5) unblocks the stalled R1000 steward plan.** Planning
  stopped at the uncertainty gate; it can resume now with the ruled scope:
  the formatting rule and its blind unit tests asserting exact printed strings
  — including the ESC-1 reproduction, the 0.1 km → m artifact value formatting
  to exactly `100`, red against the artifact and green against the fix — and
  **nothing else**. No `anvil` command, no CLI syntax decision, no acceptance
  scripts. The steward should cite OD-1 and OD-4 together.

## Constraints on later plans (not work to dispatch now)

- Milestone plan `convert` delivers `acceptance/S1.sh` in the same plan as the
  command it invokes, carrying OD-1's exact-stdout-string constraint verbatim
  and the 0.1 km → m row in S1's fixed table. After S1 lands and runs green,
  the `convert` implementer appends the ESC-1 correction row in
  `docs/escapes.md`.
- Milestone plan `temperature` delivers `acceptance/S2.sh` the same way.
- No acceptance script lands before the command it runs exists. Until then,
  `acceptance-criteria.sh`'s per-PR "no script yet" report is the visibility
  that stands in — do not "fix" that report with placeholder scripts.

## Do not act on yet

- The exact CLI syntax and the batch line format remain open (`docs/DESIGN.md`
  §11); OD-4 deliberately does not narrow them. The `convert` planner files
  them as uncertainties when planning reaches them.
- Two Proposed backlog items (unit aliases; temperatures below absolute zero)
  remain unruled. They are deliberately not named by id here, for the same
  reason as in run 1: the driver records every id a handoff mentions as
  processed evidence, and these have not been processed.
