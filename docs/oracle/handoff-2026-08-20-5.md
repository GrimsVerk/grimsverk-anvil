# Oracle handoff — 2026-08-20, run 5

Scope commissioned: BL-8. Resolved in `docs/DESIGN.oracle.md` as **OD-7**,
adding **R1003**. Written once; corrections go in the next-numbered file.

## Needs planning

- **The `convert-batch` milestone plan** (R6 + R1003) is now unblocked — it was
  the last planning gap `coverage.sh` reports. OD-7 fixes everything BL-8 said
  the plan was waiting on: the request-line grammar, the error-line shape
  (`anvil: line <n>: <reason>`), the stream (standard output, in input order),
  and the exit code (1 if any line failed; 2 stays R1001's usage path). The
  plan's Signatures block for the batch entry point and `acceptance/S5.sh` can
  both be written now, and the plan must list **R6 and R1003** in its `covers`.
- One wording item is deliberately left to that plan, exactly as R4 leaves
  single-shot wording to the MVP plan: the exact text of the new
  malformed-line reason. Constraint carried from §13 S5: the error line must
  name the offending input.
- S5's required content is spelled out in OD-7's measurement paragraph — the
  five-line mixed batch exact-match with the error line in stream position, a
  blank line pinning skip-plus-numbering, a malformed line, both exit codes,
  and one batch result line byte-identical to single-shot.

## Existing plans this touches

None. `docs/plans/oracle/anvil-convert-mvp.md` and
`docs/plans/oracle/anvil-temperature.md` are unchanged by OD-7 — the MVP's
"`--batch` is a usage error until the batch milestone lands" stance is exactly
what R1001 and R1003 together still say. Build order stands: MVP first,
temperature second, batch third.

## Do NOT act on

- **BL-3 (OD-2, HALTED)** — unchanged: blocked on the owner reconciling BL-3
  with tenet V5. Do not plan it or approximate it.
- **BL-4 (OD-3, rejected)** — unchanged: currency stays a non-goal.
- **BL-1, BL-2** — Proposed, owner-filed, not commissioned: they move only when
  the owner moves them.

## Notes for the orchestrator

- ESC-1 remains open (stub) until OD-1's item (c) — the correction row citing
  the merged `acceptance/S1.sh` — lands as its own one-line `docs/` pull
  request after the MVP merges. Unchanged from handoff run 1.
- OD-7 takes the stream question (error lines to standard output, not standard
  error) against the Unix default, on V2/V1 grounds recorded in the decision.
  If the owner wants the conventional split instead, the sentence to edit is
  V2; the reversal cost before S5 lands is a plan edit, after it, one
  superseding entry plus a rewrite of S5 and the batch code together — the
  HIGH-risk cost BL-8 itself named, which is why this was ruled now.
